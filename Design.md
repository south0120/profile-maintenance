# Design.md — Coca-Cola Japan 公式サイト トップページ再現用デザイン仕様書

対象URL: https://www.coca-cola.com/jp/ja
版: v1.0
作成日: 2026-08-05
目的: 日本コカ・コーラ公式サイトのトップページ（および共通ヘッダー／フッター）を学習・検証目的で再現するためのデザイン仕様を定義する。

> **注記（検証状態）**: 本仕様書は coca-cola.com が採用するグローバル共通デザインシステムの構成知識および公開情報に基づいて作成した。作成環境のネットワーク制約により実サイトのライブ取得・スクリーンショット検証は未実施のため、実装時は実サイトと見比べて細部（実際のキャンペーン内容、ナビ項目の最新状態、正確な余白数値）を調整すること。
>
> **注記（知的財産権）**: Coca-Cola のロゴ・ブランド名・製品画像・広告文言はザ コカ・コーラ カンパニーの商標・著作物である。再現は私的な学習目的に留め、ロゴや実画像はダミー画像・プレースホルダーテキストに置き換えること。公開・配布は行わないこと。

---

## 1. サイト全体の設計思想

- **グローバル共通プラットフォーム**: coca-cola.com は国・言語ごとのパス（`/jp/ja` など）で切り替わる単一のデザインシステムで構築されている。コンポーネントは国をまたいで共通で、コンテンツ（バナー・記事・製品）だけが差し替わる。
- **コンテンツドリブン**: トップページは「ヒーロー → ブランド一覧 → キャンペーン → 記事・ストーリー」という、カード／カルーセル中心の縦積み構成。
- **ブランドカラー主導**: コカ・コーラレッドをアクセントに、地はほぼ白／黒のみ。彩度の高い色は写真・製品ビジュアル側に持たせ、UI 自体は極めてミニマル。
- **モバイルファースト**: 全セクションが1カラム縦積み→ブレークポイントでカラム数が増える設計。カルーセルはモバイルでは横スワイプ。

---

## 2. デザイントークン

### 2.1 カラー

| トークン | 値 | 用途 |
|---|---|---|
| `--color-primary` | `#F40009` | コカ・コーラレッド。CTAボタン、アクセント、フォーカスリング |
| `--color-primary-dark` | `#C00008` | ボタン hover |
| `--color-black` | `#000000` | フッター背景、見出しテキスト |
| `--color-text` | `#1E1E1E` | 本文テキスト |
| `--color-text-muted` | `#6B6B6B` | 補助テキスト、日付、タグ |
| `--color-white` | `#FFFFFF` | ページ背景、ヘッダー背景 |
| `--color-surface` | `#F7F7F7` | セクション区切りの薄グレー背景、カード地 |
| `--color-border` | `#E0E0E0` | 罫線、カード枠 |
| `--color-focus` | `#F40009` | フォーカス可視化（2pxアウトライン） |

### 2.2 タイポグラフィ

実サイトは専用フォント（TCCC UnitySans / UnityHeadline 系）＋日本語ゴシックを使用。再現時は入手可能な代替で構成する。

```css
--font-body: "Noto Sans JP", "Hiragino Kaku Gothic ProN", "Yu Gothic",
             system-ui, sans-serif;
--font-heading: "Noto Sans JP", sans-serif; /* weight 700–900 で代替 */
```

| 要素 | サイズ（PC / SP） | ウェイト | 備考 |
|---|---|---|---|
| H1（ヒーロー見出し） | 40–48px / 28px | 700–900 | 行間 1.2 |
| H2（セクション見出し） | 28–32px / 22px | 700 | 中央寄せまたは左寄せ |
| H3（カード見出し） | 18–20px / 16px | 700 | 2行で省略（line-clamp: 2） |
| 本文 | 16px / 14px | 400 | 行間 1.7 |
| タグ・キャプション | 12–13px | 500 | letter-spacing 0.05em、大文字（英字時） |
| ナビゲーション | 14–15px | 500 | |

### 2.3 スペーシング・形状

| トークン | 値 |
|---|---|
| 基本スペーシング単位 | 8px（8 / 16 / 24 / 32 / 48 / 64 / 96） |
| コンテンツ最大幅 | 1280px（`margin: 0 auto; padding: 0 24px`） |
| セクション上下余白 | PC 64–96px ／ SP 40–48px |
| カード角丸 | 16px（画像・カードとも大きめのラウンド） |
| ボタン角丸 | 9999px（完全なピル型） |
| カードシャドウ | `0 2px 8px rgba(0,0,0,.08)`、hover 時 `0 6px 16px rgba(0,0,0,.14)` |

### 2.4 ブレークポイント

| 名称 | 幅 | 主な変化 |
|---|---|---|
| Mobile | 〜767px | 1カラム、ハンバーガーメニュー、カルーセルはスワイプ |
| Tablet | 768–1023px | カード2カラム、ナビは折りたたみ継続 |
| Desktop | 1024px〜 | カード3〜4カラム、水平グローバルナビ＋メガメニュー |

---

## 3. 共通レイアウト

### 3.1 ヘッダー（全ページ共通）

```
┌────────────────────────────────────────────────────────┐
│ [☰(SPのみ)]  ナビ: ブランド ▾  キャンペーン  記事 ▾ … │
│        中央: Coca-Cola スクリプトロゴ（ダミーに置換）      │
│        右: [検索 🔍] [国・言語 🌐] [ログイン 👤]           │
└────────────────────────────────────────────────────────┘
```

- 白背景、高さ PC 約 72px ／ SP 約 56px、`position: sticky; top: 0`、下辺に 1px ボーダーまたは薄いシャドウ。
- **ロゴ**: 中央配置（Desktop）。SP では左寄せ（ハンバーガーの右隣）。クリックでトップへ。
- **グローバルナビ（Desktop）**: 左側に水平配置。主要項目の例:
  - ブランド（メガメニュー: カテゴリ別ブランドロゴ一覧を全幅パネルで表示）
  - キャンペーン
  - 記事／ストーリー（ドロップダウン）
  - 企業情報・メディアセンター
- **右側ユーティリティ**: 検索アイコン（クリックで全幅の検索バーが展開）、国・言語セレクタ（モーダルで国リスト表示）、ログイン／会員アイコン。
- **SP**: ハンバーガー → 全画面ドロワー。ナビ項目はアコーディオン展開。
- ホバー: ナビ項目に赤の下線インジケータ（2px）をスライドイン。

### 3.2 フッター（全ページ共通）

```
┌────────────────────────────────────────────────────────┐
│ 背景 #000（白文字）                                       │
│ ロゴ（白抜き・ダミー）                                     │
│ ┌──────────┬──────────┬──────────┬──────────┐            │
│ │ 会社情報   │ サポート  │ 関連サイト │ SNS       │            │
│ │ ・企業情報 │ ・お問合せ │ ・採用情報 │ [X][IG]   │            │
│ │ ・ニュース │ ・FAQ     │ ・IR      │ [YT][FB]  │            │
│ └──────────┴──────────┴──────────┴──────────┘            │
│ [🌐 日本 - 日本語] （国・言語切替）                          │
│ ─────────────────────────────────────────                │
│ 利用規約 ／ プライバシーポリシー ／ Cookie設定 ／ 特商法表記   │
│ © 20XX The Coca-Cola Company（表記はダミーに）              │
└────────────────────────────────────────────────────────┘
```

- リンク列は PC 4カラム／SP はアコーディオン（タップで開閉）。
- リンク文字 14px、hover で下線。SNS アイコンは白の線画アイコン 24px。
- 最下段に法的リンクの横並び＋コピーライト。

### 3.3 Cookie 同意バナー

- 初回訪問時に画面下部へ固定表示。白背景・シャドウ付き。
- 説明文＋「すべて受け入れる」（赤ピルボタン）／「設定」（アウトラインボタン）。
- 再現時は localStorage に同意フラグを保存して非表示化する程度で十分。

---

## 4. トップページ セクション構成（上から順）

### 4.1 ヒーローカルーセル

- ファーストビュー全幅（最大幅制限なし）。アスペクト比 PC 約 21:9〜16:7、SP 約 4:5 の別画像。
- スライド構成: 背景画像（キャンペーンビジュアル）＋左下または中央に見出し・サブテキスト・CTAボタン（赤ピル「詳しくみる」等のダミー文言）。
- 3〜5枚を自動再生（5〜7秒間隔）。左右矢印（円形・半透明白地）、下部中央にドットインジケータ（アクティブは赤）。
- 一時停止ボタンを備える（アクセシビリティ要件）。
- SP はスワイプ操作。矢印は非表示可。

### 4.2 ブランドカルーセル（「ブランド一覧」）

- セクション見出し（例: 「ブランド」）＋「すべて見る」リンク（→ `/jp/ja/brands` 相当のページ）。
- **円形サムネイル**の横スクロールカルーセル。各アイテム = 円形のブランドビジュアル（直径 PC 約 160px／SP 約 112px）＋直下にブランド名テキスト。
- 掲載ブランドの例（日本市場。ロゴはダミー画像で代替）:
  - コカ・コーラ ／ コカ・コーラ ゼロ
  - ファンタ ／ スプライト
  - ジョージア（コーヒー）
  - 綾鷹（緑茶）
  - い・ろ・は・す（水）
  - アクエリアス（スポーツ飲料）
  - リアルゴールド ほか
- hover でサムネイルがわずかに拡大（scale 1.05）。クリックで各ブランドページへ。
- PC では 6〜7個表示＋左右矢印、SP では 2.5個見せのスワイプ。

### 4.3 キャンペーン／プロモーションセクション

- 見出し（例: 「キャンペーン」）＋横並びカードグリッド（PC 2〜3カラム、SP 1カラム）。
- カード = 横長ビジュアル（16:9、角丸16px）＋タグ（「キャンペーン」等）＋見出し＋期間テキスト。
- アプリ連携訴求（Coke ON 等）のバナーが含まれることがある。再現時はダミーバナー1枚で可。

### 4.4 記事・ストーリーセクション

- 見出し（例: 「最新の記事」）＋「もっと見る」リンク。
- カードグリッド: PC 3〜4カラム／Tablet 2カラム／SP 1カラム（または横スクロール）。
- カード構成:
  1. サムネイル画像（16:9、角丸、hover でズーム `scale(1.05)` を `overflow: hidden` 内で）
  2. カテゴリタグ（小さめ・グレーまたは赤文字）
  3. 記事タイトル（H3、2行省略）
  4. 公開日（muted）
- カード全体がリンク。フォーカス時は赤アウトライン。

### 4.5 下部 CTA セクション

- ページ末尾（フッター直前）に、会員登録・アプリダウンロード・メールマガジン等の訴求ブロック。
- 薄グレーまたは赤ベタの帯に、見出し＋説明文＋赤/白ピルボタン。再現時はダミー文言のシンプルな1ブロックで可。

---

## 5. コンポーネント仕様

### 5.1 ボタン

| 種別 | 通常 | hover | 備考 |
|---|---|---|---|
| Primary | 背景 `#F40009`、白文字、ピル型、padding `12px 32px` | 背景 `#C00008`、わずかに拡大 | CTA用 |
| Secondary | 白背景、黒 or 赤の 2px ボーダー、ピル型 | 背景が薄赤/薄グレー | 「設定」等の副次操作 |
| テキストリンク | 黒文字＋矢印アイコン | 赤文字＋下線 | 「すべて見る」等 |

### 5.2 カード

- 構造: `<a>` 全体リンク > 画像ラッパー（角丸・overflow hidden）> テキスト部。
- 遷移: `transition: transform .3s ease, box-shadow .3s ease`。
- 画像は `aspect-ratio` で固定し、`object-fit: cover`。

### 5.3 カルーセル

- 実装: CSS scroll-snap（`scroll-snap-type: x mandatory`）＋左右ボタンで `scrollBy`。ライブラリを使う場合は Swiper 相当。
- 矢印ボタン: 円形 44px、白地・シャドウ、無効時は非表示。
- ドット: 8px 円、アクティブは赤・横長（24px）に変形。
- 自動再生はヒーローのみ。`prefers-reduced-motion` で停止。

### 5.4 タグ／ラベル

- 12px、weight 500、色 `--color-text-muted` または赤。カテゴリ区別のための接頭ラベルとして使用。

---

## 6. レスポンシブ挙動まとめ

| 要素 | Mobile | Tablet | Desktop |
|---|---|---|---|
| ヘッダー | ハンバーガー＋左ロゴ | 同左 | 水平ナビ＋中央ロゴ |
| ヒーロー | 4:5 画像・スワイプ | 16:9 | 21:9・矢印あり |
| ブランド | 2.5個見せスワイプ | 4〜5個 | 6〜7個＋矢印 |
| 記事グリッド | 1カラム | 2カラム | 3〜4カラム |
| フッターリンク | アコーディオン | 2カラム | 4カラム |

---

## 7. アクセシビリティ

- すべての画像に `alt`（装飾画像は空 alt）。
- カルーセルは `aria-roledescription="carousel"`、スライドに `aria-label="n / N"`、自動再生の一時停止ボタン必須。
- キーボード: ナビ・カード・カルーセル矢印すべて Tab 到達可能、フォーカスリングは赤 2px を明示（`:focus-visible`）。
- コントラスト: 赤 `#F40009` 上の白文字は大きめサイズ（18px 以上/太字）で使用。本文は黒系でコントラスト比 4.5:1 以上を担保。
- ドロワー／モーダル（検索・言語選択）はフォーカストラップ＋ `Esc` で閉じる。

---

## 8. 実装ガイド

### 8.1 推奨構成

- **最小構成**: 静的 HTML + CSS（+ 少量の Vanilla JS: ドロワー、カルーセル、Cookie バナー）。単一 `index.html` で再現可能。
- **拡張構成**: 本リポジトリと同じ Vite + React + TypeScript 構成。セクションをコンポーネント分割（`Header` / `HeroCarousel` / `BrandCarousel` / `CardGrid` / `Footer`）し、コンテンツは JSON データから描画するとグローバル共通テンプレートの思想に近づく。

### 8.2 アセットの扱い

- ロゴ・製品画像・キャンペーンビジュアルは使用しない。`https://placehold.co/` 等のプレースホルダー画像、または赤系のグラデーション矩形＋テキストで代替する。
- ブランドの円形サムネイルは、ブランドごとのイメージカラー（赤・オレンジ・緑・青など）のベタ円＋ブランド名テキストで表現すると雰囲気が再現できる。
- 文言はすべてダミー（「キャンペーンタイトルが入ります」等）とする。

### 8.3 実装順序の目安

1. デザイントークンを CSS カスタムプロパティとして定義（§2）
2. ヘッダー／フッターの共通レイアウト（§3）
3. トップページ各セクションを上から順に実装（§4）
4. カルーセル・ドロワー等のインタラクション（§5）
5. レスポンシブ調整（§6）→ アクセシビリティ確認（§7）

---

## 9. 実サイト確認時のチェックリスト

ライブ検証が可能になった際に、以下を実サイトと突き合わせて本書を更新すること。

- [ ] グローバルナビの正確な項目名と順序、メガメニューの内容
- [ ] ヒーローカルーセルの枚数・アスペクト比・自動再生間隔
- [ ] トップページのセクション順序と見出し文言
- [ ] ブランドカルーセルの掲載ブランドと並び順
- [ ] フッターのリンク列構成と法的リンク
- [ ] 正確なフォント指定・フォントサイズ・余白数値（DevTools で採取）
